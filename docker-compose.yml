services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: harriputtar/portfolio-app:latest
    container_name: portfolio-app
    ports:
      - "8080:80"
    restart: unless-stopped
